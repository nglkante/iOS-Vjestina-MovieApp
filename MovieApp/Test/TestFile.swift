//let ratingView = RatingView(rating: rating)
//view.addSubview(ratingView)
//
//let nameAndYearView = NameAndYearView(name: name, year: year)
//view.addSubview(nameAndYearView)
//
//let releaseDateView = ReleaseDateView(releaseDate: releaseDate)
//view.addSubview(releaseDateView)
//
//let categoriesAndDurationView = CategoriesAndDurationView(categories: categories, duration: duration)
//view.addSubview(categoriesAndDurationView)
//
//let summaryView = SummaryView(summary: summary)
//view.addSubview(summaryView)

//let collectionView: UICollectionView = {
//        let layout = UICollectionViewFlowLayout()
//        layout.scrollDirection = .horizontal //Change this to vertical
//        layout.minimumLineSpacing = 8 //Spacing here is not necessary, but adds a better inset for horizontal scrolling. Gives you a tiny peek of the background. Probably not great for vertical
//        layout.minimumInteritemSpacing = 0
//        layout.sectionInset = UIEdgeInsets(top: 0, left: 4, bottom: 0, right: 4)
//        let coll
